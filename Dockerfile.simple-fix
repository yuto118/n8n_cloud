FROM n8nio/n8n:latest

# 環境変数
ENV N8N_HOST=0.0.0.0
ENV N8N_PORT=8080
ENV NODE_ENV=production

# ポート公開
EXPOSE 8080

# ヘルスチェックを無効化
HEALTHCHECK NONE

# n8nを直接8080ポートで起動
CMD ["sh", "-c", "echo 'n8n starting on port 8080...' && exec n8n start"] 